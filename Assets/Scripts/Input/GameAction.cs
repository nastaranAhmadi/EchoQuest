namespace EchoQuest.Input
{
    /// <summary>
    /// Semantic gameplay actions. Device-specific input maps to these values.
    /// </summary>
    public enum GameAction
    {
        None = 0,
        MoveUp,
        MoveDown,
        MoveLeft,
        MoveRight,
        Interact,
        Cancel,
        Pause,
        Select,
        Back
    }
}
